import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingHorizontal: 20
    },
    content: {
        // space for list on top
        paddingTop: 135,
        // space for any footer element on screen
        paddingBottom: 184
    }
});